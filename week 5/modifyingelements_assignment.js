const box = document.getElementById("theText")
const newValue = function(){
    const button = document.getElementById("submit").addEventListener("click", newValue)
    if(box.value === "") {
        alert(box.value)
    }
}
